{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="card">
        <div class="header">
            <h4 class="title">Vous créez un article dans la sous-catégorie <span class="text-danger">{{ sousCategorie.titre }}</span></h4>
        </div>
    </div>
    <!-- CREER SOUS CATEGORIE -->


            {{ form_start(formArticle) }}


            {{ form_row(formArticle.titre) }}

            {{ form_row(formArticle.descriptif) }}

            {{ form_row(formArticle.contenu) }}

            <button style="background-color: #5cb85c; color: white; border: 2px solid #5cb85c; border-radius: 4px; padding: 15px" type="submit" class="btn btn-primary">Créer</button>

            {{ form_end(formArticle) }}


{% endblock %}